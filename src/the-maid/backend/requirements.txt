# The Maid — Python Backend Dependencies
# Core
fastapi==0.141.1
uvicorn==0.52.0
# uvicorn[standard] extras pinned explicitly so pip-audit can audit them directly
# without resolving extras at audit time (avoids network timeouts in the audit step).
httptools==0.8.0
python-dotenv==1.2.2
PyYAML==6.0.3
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.0.1
pydantic==2.9.2
python-multipart==0.0.32
starlette==1.3.1

# File Processing
PyMuPDF==1.24.14
python-docx==1.1.2
openpyxl==3.1.5
Pillow==12.3.0

# Metadata
PyExifTool==0.5.6

# AI/ML (bundled tier)
# llama-cpp-python intentionally removed — it forced a vulnerable diskcache
# transitive dependency (PYSEC-2026-2447). We now call a local llama.cpp
# HTTP server instead. See the_maid/models.py for details.
onnxruntime==1.19.2

# Face Recognition (downloadable)
insightface==1.0.1
scikit-learn==1.5.2
numpy==2.0.2

# Utils
aiofiles==24.1.0
python-magic==0.4.27
requests==2.33.0
