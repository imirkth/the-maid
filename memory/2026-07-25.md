# Daily Raw Log — 2026-07-25

## Slice 4A/4B Approval UI Bug Review (subagent)

- Read CONTEXT.md, ADR 0005, diagnosing-bugs skill.
- Read target files: ApprovalView.tsx, approval.ts, approval-advanced.test.ts, cleanup_plan.py, categorizer.py, sandbox.py.
- Built red-capable feedback loop by adding regression tests before fixing.
- Reproduced 6 bugs (2 High, 3 Medium, 2 Low including build).
- Fixes applied:
  - `applyFieldApprovals()` clears `user_edited_path` when move is false.
  - Added `buildFinalProposals()` to gate field approvals by `advancedMode`.
  - `reassignBucket()` and inline edit promote `tag`/`delete` to `move`.
  - Rewrote `validateEditedPath()` to resolve `~`, collapse `..`, resolve relative sandbox names against home, and use exact-or-`/` prefix containment for system dirs.
  - Fixed system-dir prefix bug in both TS (`approval.ts`) and Python (`sandbox.py`).
  - Improved accessibility: labels, keyboard handlers, roles, live regions.
  - Fixed `index.html` asset paths so Vite build succeeds.
- Test results: vitest 75 passed (was 64), pytest 96 passed.
- Production build: `npm run build` succeeds.
- Posted findings with severity to GitHub issue #7: https://github.com/imirkth/the-maid/issues/7#issuecomment-5078111122
- Committed: `f8e62e2 fix(Slice 4A/4B): Approval UI bugs — ...`
- ByteRover cloud sync unavailable (auth required); lesson captured in commit and todo.md.
