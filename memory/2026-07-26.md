
## [15:19] Slice 1 (#16) completed
- Spawned Coding sub-agent `coder_slice_1_vendor_url` for issue #16.
- Sub-agent ran isolated; changes already converged with existing workspace state.
- Verified acceptance criteria: build-time `DONATION_LNURL`, placeholder guard, Tauri invoke registration, Rust test, frontend error surfacing.
- TypeScript clean, 140 frontend tests passed.
- Closed GitHub issue #16 and posted Discord summary.
- Next: issue #17 (Generate and display BOLT11 invoice).

## [17:12] Tester sub-agent fix pass: Lightning donation bugs #20–#24
- Read relevant files: `SettingsPanel.tsx`, `lightning.rs`, `donation.ts`, ADR 0011, sub-agent spawn rules.
- Phase 1: built tight feedback loops.
  - Frontend: installed `@testing-library/react`, `jsdom`; configured `vitest.config.ts` with node default + per-file `// @vitest-environment jsdom`.
  - Rust: standalone harness `/tmp/maid-lightning-test` because full Tauri build fails on missing GTK dev libs.
- Phase 2–6: reproduced, hypothesised, instrumented, fixed, and added regression tests for all 5 bugs.
  - Bug #20: extracted `useDonation` hook; added `creatingRef` guard against concurrent `createInvoice` calls.
  - Bug #21: `copyInvoice()` now awaits and catches `navigator.clipboard.writeText()` errors.
  - Bug #22: validate `expires_at` parse; malformed values disable verification instead of producing `NaN`.
  - Bug #23: removed fake `payment_hash` fallback; require LNURL response to include real `payment_hash`.
  - Bug #24: added `reason` to `PaymentStatus`; frontend stops polling and surfaces terminal LNURL errors.
- Updated ADR 0011 to reflect payment_hash requirement, expiry validation, and terminal error handling.
- Verification:
  - `npx tsc --noEmit`: clean.
  - `npm test -- --run`: 150 frontend tests pass.
  - Rust standalone harness: 15 passed, 1 ignored (placeholder test).
- Commits:
  - `3dff4a8` fix(donation): bugs #20, #21, #22, #24 frontend
  - `1fdadb0` fix(donation): bugs #23, #24 Rust
  - `5611ae6` docs(adr): update ADR 0011
- Closed GitHub issues #20, #21, #22, #23, #24 with fix comments.
- Posted Discord summary to #project-the-maid.
