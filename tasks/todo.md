# Task: Lightning Donation (Slice 7 extension) — DONE

- Switched from user-configurable node to vendor node URL baked in at build time.
- Build-time env: `MAID_DONATION_LNURL=https://your-node.example/lnurl-pay cargo tauri build`
- Commit: see `git log`.
